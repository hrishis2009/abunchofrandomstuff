{
    "scripts": {
        "test": [
            "@php php/login-signup/create-db.php",
            "phpunit"
        ]
        - "name" : "php.yml"
        - "description" : "Install php and use with composer"
    }
}
