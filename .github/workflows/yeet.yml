name: Install Workflows

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install node software
        uses: actions/setup-node@v1
        
      - name: Install PHP action
        run: npm install setup-php
        
        
